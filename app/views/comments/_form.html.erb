<%= form_for([@post, @comment]) do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :body %>
	<%= f.text_field :body %>

	<%= f.label :email %>
	<%= f.text_field :email %>

	<%= f.submit %>

<% end %>